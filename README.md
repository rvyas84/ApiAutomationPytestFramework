# 🚀 Quick Start

This repository demonstrates how to perform REST API automation based on Python using frameworks such as PyTest.

# 📌 Features
- Simple API test structure using plain Python
- Uses the requests library for HTTP calls
- JSON validation using built-in json module
- Reusable helper functions for GET/POST/PUT/DELETE requests
- Easy to extend for CI/CD pipelines
- pytest-html reporting

# 📦 Requirements
Install required dependencies:
    
    ```bash
    pip install -r requirements.txt
    ```

# 🚀 How to Run Tests

You can run tests simply using:
    
    ```bash
    pytest -s
    ```